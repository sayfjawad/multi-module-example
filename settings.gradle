rootProject.name = 'multi-module'
include 'service', 'core', 'generate'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("libs.versions.toml"))
        }
    }
}